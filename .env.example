# MCP Server Configuration
PORT=8080

# Transport Selection
# Options: stdio (default) | http
TRANSPORT_MODE=stdio

# HTTP Transport Configuration (only used when TRANSPORT_MODE=http)
HTTP_ENDPOINT=/mcp

# Sunsama Authentication
# For stdio transport: Required
# For HTTP transport with Basic Auth: Provided via HTTP Basic Auth per request
# For HTTP transport with token auth: Required (server uses these credentials)
SUNSAMA_EMAIL=your-email@example.com
SUNSAMA_PASSWORD=your-password

# Optional Token Authentication for HTTP Transport
# When set, enables token-based authentication for remote Claude Desktop connections
# Clients can authenticate using:
#   1. Query parameter: /mcp?token=your-secret-token
#   2. Bearer token: Authorization: Bearer your-secret-token
#   3. HTTP Basic Auth (fallback): Authorization: Basic base64(email:password)
# If not set, only HTTP Basic Auth is accepted
# Generate a secure token: openssl rand -base64 32
MCP_AUTH_TOKEN=